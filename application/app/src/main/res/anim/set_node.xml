<?xml version="1.0" encoding="utf-8"?>
<rotate xmlns:android="http://schemas.android.com/apk/res/android">
    <rotate
            android:fromDegrees="0"
            android:toDegrees="-90"
            android:pivotX="50%"
            android:pivotY="50%"
            android:duration="2000"
            android:repeatCount="1"
            android:repeatMode="reverse">
    </rotate>
    <!--
    fromDegrees="0"     :起始角度
    toDegrees="359"     ：结束角度，负数表示逆时针，正数表示顺时针
    pivotX="50%"        ：旋转中心距离view的左顶点为50%距离
    pivotY="50%"        ：旋转中心距离view的上边缘为50%距离
    duration="2000"     ：从android:fromDegrees转动到android:toDegrees所花费的时间，单位为毫秒
    repeatCount="1"     ：重复的次数，默认为0，必须是int，可以为-1表示不停止
    android:repeatMode  ：重复的模式，默认为restart，即重头开始重新运行，可以为reverse即从结束开始向前重新运行。在android:repeatCount大于0或为infinite时生效
    -->
</rotate>